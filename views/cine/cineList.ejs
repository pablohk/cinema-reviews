<h2> Cine List<h2>
  <% if(typeof(items) !== 'undefined'){
    items.forEach(e =>{%>
      <div> <a href="/cine/<%=e._id%>/detail"><%=e.name%></a></div>
      <div> <%=e.address.street%></div>
      <div> <%=e.phone%></div>
    <%});%>
  <%}else{%>
    <% if ((message) !== "undefined") { %>
    <div class="error-message"><%= message%></div>
    <% }
  }%>
  
